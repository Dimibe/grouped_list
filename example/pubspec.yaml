name: example
description: grouped_list example.
publish_to: 'none' 

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  grouped_list:
    path: ../
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=2.0.3 <5.0.0"

flutter:
  uses-material-design: true
